<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Survey Response PDF</title>
  <style>
    /* Base typography */
    @page { margin: 1.2cm 1.2cm; }
    html, body { font-family: "Helvetica Neue", Helvetica, Arial, sans-serif; color: #24303a; font-size: 13px; }

    /* Header */
    .pdf-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 12px; }
    .pdf-brand { font-size: 18px; font-weight: 800; color: #500000; }
    .pdf-meta { text-align: right; font-size: 12px; color: #586069; line-height: 1.4; }

    /* Info box */
    .user-info { border: 1px solid #e2e8f0; background: #ffffff; padding: 10px 12px; border-radius: 10px; margin-bottom: 14px; display: flex; gap: 20px; }
    .user-info .col { min-width: 160px; }
    .user-info .label { font-size: 11px; color: #6b7785; margin-bottom: 4px; text-transform: uppercase; letter-spacing: 0.04em; }
    .user-info .value { font-weight: 600; color: #102a43; }

    h1.survey-title { font-size: 20px; margin: 0 0 8px 0; color: #0f172a; }
    h2.section-title { font-size: 16px; margin: 18px 0 10px 0; color: #0f172a; }
    h3.category-title { font-size: 14px; margin: 0 0 6px 0; color: #0f172a; }

    .metrics-row { display: flex; flex-wrap: wrap; gap: 10px; margin-bottom: 16px; }
    .metric-card { border: 1px solid #e2e8f0; background: #ffffff; padding: 10px 12px; border-radius: 10px; min-width: 150px; }
    .metric-label { font-size: 11px; color: #6b7785; text-transform: uppercase; letter-spacing: 0.04em; margin-bottom: 4px; }
    .metric-value { font-size: 14px; font-weight: 600; color: #102a43; }

    .pdf-section { margin-bottom: 18px; }
    .section-card { border: 1px solid #fcd34d; border-radius: 12px; padding: 12px 14px; margin: 12px 0; background: rgba(251, 191, 36, 0.12); }
    .section-kicker { font-size: 11px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.06em; color: #92400e; margin-bottom: 4px; }
    .section-name { font-size: 16px; font-weight: 800; color: #0f172a; }
    .section-description { font-size: 12px; color: #334155; }
    .section-description p { margin: 6px 0 0 0; }

    .category-block { border: 1px solid #e2e8f0; border-radius: 12px; padding: 12px 14px; margin-bottom: 12px; background: #ffffff; }
    .category-header { margin-bottom: 6px; }
    .category-description { font-size: 12px; color: #475569; }
    .category-description p { margin: 0 0 6px 0; }

    .question-block { padding: 10px 0; border-bottom: 1px solid #e2e8f0; page-break-inside: avoid; }
    .question-header { display: flex; gap: 8px; align-items: flex-start; margin-bottom: 4px; }
    .question-number { color: #500000; font-weight: 800; min-width: 28px; }
    .question-heading { display: flex; justify-content: space-between; gap: 10px; width: 100%; }
    .question-body { font-weight: 700; color: #0f172a; }
    .target-level-wrap { margin-left: auto; text-align: right; white-space: nowrap; }
    .target-level-badge { display: inline-block; padding: 4px 10px; border-radius: 999px; font-size: 11px; font-weight: 700; color: #0f172a; background: #f1f5f9; border: 1px solid #e2e8f0; }
    .question-description { font-size: 12px; color: #475569; margin: 4px 0 6px 36px; }
    .question-description p { margin: 0 0 6px 0; }
    .question-answer { font-size: 12px; color: #102a43; margin-bottom: 4px; }
    .question-meta { font-size: 11px; color: #6b7280; display: flex; gap: 12px; }

    .choice-list { list-style: none; padding: 0; margin: 6px 0 0 0; }
    .choice-item { display: inline-flex; align-items: center; gap: 6px; padding: 3px 6px; border-radius: 4px; border: 1px solid transparent; margin-right: 6px; font-size: 12px; }
    .choice-item.selected { background: rgba(80, 0, 0, 0.08); border-color: rgba(80, 0, 0, 0.2); font-weight: 700; }
    .choice-marker { font-size: 12px; color: #500000; }

    .answer-text { white-space: pre-wrap; word-break: break-word; }

    .pdf-select { width: 100%; max-width: 320px; border: 1px solid #cbd5e1; border-radius: 8px; background: #ffffff; padding: 6px 10px; font-size: 12px; color: #0f172a; }
    .pdf-select:disabled { background: #ffffff; color: #0f172a; opacity: 1; }
    .history-label { font-size: 11px; font-weight: 600; margin-top: 6px; }
    .history-list { margin: 4px 0 0 14px; padding: 0; font-size: 11px; color: #475569; }
    .history-list li { margin-bottom: 2px; }

    .badge { display: inline-block; padding: 2px 6px; border-radius: 10px; font-size: 10px; text-transform: uppercase; letter-spacing: 0.04em; }
    .badge-warning { background: #fef3c7; color: #92400e; }

    .empty-state { font-size: 12px; color: #6b7280; font-style: italic; }
  .truncation-note { font-size: 11px; color: #94a3b8; margin-top: 4px; }

    .feedback-category { border: 1px solid #e2e8f0; border-radius: 12px; padding: 12px 14px; margin-bottom: 12px; background: #ffffff; }
    .feedback-block { border: 1px solid #e2e8f0; border-radius: 10px; padding: 10px 12px; margin-bottom: 8px; background: #f8fafc; }
    .feedback-header { display: flex; justify-content: space-between; gap: 10px; font-size: 12px; color: #334155; margin-bottom: 6px; }
    .feedback-score { font-weight: 800; color: #500000; }
    .feedback-comments { font-size: 12px; color: #102a43; }
    .feedback-comments p { margin: 0 0 6px 0; }
    .feedback-empty { font-size: 12px; color: #64748b; font-style: italic; }
    .feedback-meta { margin-top: 4px; font-size: 11px; }

    /* Footer */
    .pdf-footer { position: fixed; bottom: 6px; left: 0; right: 0; text-align: center; font-size: 11px; color: #8b99a6; }

    /* Small helpers */
    .muted { color: #6b7785; }
  </style>
</head>
<body>
  <div class="pdf-header">
    <div class="pdf-brand"><%= content_for?(:pdf_brand) ? yield(:pdf_brand) : "Health — Survey Response" %></div>
    <div class="pdf-meta"><%= content_for?(:pdf_meta) ? yield(:pdf_meta) : "Generated: #{Time.current.strftime('%Y-%m-%d %H:%M')}" %></div>
  </div>

  <%= yield %>

  <div class="pdf-footer">This document was generated from the Health application.</div>
</body>
</html>
