<div id="<%= dom_id category %>">
  <p>
    <strong>Survey:</strong>
    <%= category.survey&.title || "Unassigned" %>
  </p>

  <p>
    <strong>Name:</strong>
    <%= category.name %>
  </p>

  <p>
    <strong>Description:</strong>
    <%= category.description.presence || "—" %>
  </p>
</div>
