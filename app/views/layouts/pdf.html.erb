<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Survey Response PDF</title>
  <style>
    /* Base typography */
    @page { margin: 1.2cm 1.2cm; }
    html, body { font-family: "Helvetica Neue", Helvetica, Arial, sans-serif; color: #24303a; font-size: 13px; line-height: 1.35; }
    * { box-sizing: border-box; }
    /* wkhtmltopdf fixed elements overlay content; reserve space for footer */
    body { padding-bottom: 26px; }

    /* Header */
    /* wkhtmltopdf flex support can be inconsistent; use table layout for reliability. */
    .pdf-header { display: table; width: 100%; margin-bottom: 12px; }
    .pdf-header-left { display: table-cell; vertical-align: middle; }
    .pdf-header-right { display: table-cell; vertical-align: middle; text-align: right; }

    .pdf-logo-badge { background: #500000; padding: 6px 10px; border-radius: 6px; display: inline-block; line-height: 0; vertical-align: middle; }
    .pdf-logo { height: 26px; width: auto; display: inline-block; vertical-align: middle; image-rendering: -webkit-optimize-contrast; }
    .pdf-brand { display: inline-block; vertical-align: middle; margin-left: 10px; }
    .pdf-brand { font-size: 18px; font-weight: 800; color: #500000; white-space: nowrap; word-break: keep-all; line-height: 1.1; }
    .pdf-meta { text-align: right; font-size: 12px; color: #586069; line-height: 1.4; }
    .pdf-meta-block { margin-top: -6px; margin-bottom: 10px; text-align: right; font-size: 12px; color: #586069; line-height: 1.4; }

    /* Info box (use table layout for wkhtmltopdf reliability) */
    .user-info { border: 1px solid #e2e8f0; background: #ffffff; padding: 10px 12px; border-radius: 10px; margin-bottom: 14px; display: table; width: 100%; }
    .user-info .col { display: table-cell; width: 50%; vertical-align: top; padding-right: 14px; }
    .user-info .col:last-child { padding-right: 0; }
    .user-info .label { font-size: 11px; color: #6b7785; margin-bottom: 4px; text-transform: uppercase; letter-spacing: 0.04em; }
    .user-info .value { font-weight: 600; color: #102a43; }

    h1.survey-title { font-size: 20px; margin: 0 0 8px 0; color: #0f172a; }
    h2.section-title { font-size: 16px; margin: 18px 0 10px 0; color: #0f172a; }
    h3.category-title { font-size: 14px; margin: 0 0 6px 0; color: #0f172a; }

    .metrics-row { display: flex; flex-wrap: wrap; gap: 10px; margin-bottom: 16px; }
    .metric-card { border: 1px solid #e2e8f0; background: #ffffff; padding: 10px 12px; border-radius: 10px; min-width: 150px; }
    .metric-label { font-size: 11px; color: #6b7785; text-transform: uppercase; letter-spacing: 0.04em; margin-bottom: 4px; }
    .metric-value { font-size: 14px; font-weight: 600; color: #102a43; }

    /* Advisor summary (PDF) */
    .advisor-summary { border: 1px solid #50586C; background: #DCE2F0; border-radius: 12px; padding: 12px 14px; margin-bottom: 18px; }
    /* Summary Statistics should be a normal card (no advisor palette). */
    .summary-card { border: 1px solid #e2e8f0; background: #ffffff; border-radius: 12px; padding: 12px 14px; margin-bottom: 18px; }
    .advisor-summary__header { display: flex; justify-content: space-between; align-items: baseline; gap: 10px; margin-bottom: 10px; }
    .advisor-summary__title { font-size: 14px; margin: 0; color: #0f172a; }
    .advisor-summary__hint { margin: 0; font-size: 11px; color: #6b7785; }
    /* Use table layout for wkhtmltopdf reliability (flex can stack). */
    .advisor-summary__grid { display: table; width: 100%; border-collapse: separate; border-spacing: 10px 0; }
    .advisor-summary__card { display: table-cell; width: 50%; vertical-align: top; border: 1px solid #e2e8f0; border-radius: 10px; padding: 10px 12px; background: #f8fafc; }
    .advisor-summary__cardRow { display: flex; justify-content: space-between; gap: 10px; }
    .advisor-summary__label { font-size: 11px; color: #6b7785; text-transform: uppercase; letter-spacing: 0.04em; margin-bottom: 4px; }
    .advisor-summary__percent { font-size: 11px; color: #6b7785; }
    .advisor-summary__count { font-size: 18px; font-weight: 800; color: #500000; }
    .advisor-summary__footer { margin-top: 10px; }
    .advisor-summary__note { margin: 0; font-size: 11px; color: #6b7785; }

    .pdf-section { margin-bottom: 18px; }
    .section-card { border: 1px solid #815854; border-radius: 12px; padding: 12px 14px; margin: 12px 0; background: #F9EBDE; }
    .section-kicker { font-size: 11px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.06em; color: #815854; margin-bottom: 4px; }
    .section-name { font-size: 16px; font-weight: 800; color: #0f172a; }
    .section-description { font-size: 12px; color: #334155; }
    .section-description p { margin: 6px 0 0 0; }

    .category-block { border: 1px solid #e2e8f0; border-radius: 12px; padding: 12px 14px; margin-bottom: 12px; background: #ffffff; }
    .category-header { margin-bottom: 6px; }
    .category-description { font-size: 12px; color: #475569; }
    .category-description p { margin: 0 0 6px 0; }

    .question-block { padding: 10px 0; border-bottom: 1px solid #e2e8f0; page-break-inside: avoid; }
    .question-header { display: flex; gap: 8px; align-items: flex-start; margin-bottom: 4px; }
    .question-number { color: #500000; font-weight: 800; min-width: 28px; }
    .question-heading { display: flex; justify-content: space-between; gap: 10px; width: 100%; }
    .question-body { font-weight: 700; color: #0f172a; }
    .target-level-wrap { margin-left: auto; text-align: right; white-space: nowrap; }
    .target-level-badge { display: inline-block; padding: 4px 10px; border-radius: 999px; font-size: 11px; font-weight: 700; color: #0f172a; background: #f1f5f9; border: 1px solid #e2e8f0; }
    .question-description { font-size: 12px; color: #475569; margin: 4px 0 6px 36px; }
    .question-description p { margin: 0 0 6px 0; }
    .question-answer { font-size: 12px; color: #102a43; margin-bottom: 4px; }
    .question-meta { font-size: 11px; color: #6b7280; display: flex; gap: 12px; }

    .choice-list { list-style: none; padding: 0; margin: 6px 0 0 0; }
    .choice-item { display: inline-flex; align-items: center; gap: 6px; padding: 3px 6px; border-radius: 4px; border: 1px solid transparent; margin-right: 6px; font-size: 12px; }
    .choice-item.selected { background: rgba(80, 0, 0, 0.08); border-color: rgba(80, 0, 0, 0.2); font-weight: 700; }
    .choice-marker { font-size: 12px; color: #500000; }

    .answer-text { white-space: pre-wrap; word-break: break-word; }

    .pdf-select { width: 100%; max-width: 320px; border: 1px solid #cbd5e1; border-radius: 8px; background: #ffffff; padding: 6px 10px; font-size: 12px; color: #0f172a; }
    .pdf-select:disabled { background: #ffffff; color: #0f172a; opacity: 1; }
    .history-label { font-size: 11px; font-weight: 600; margin-top: 6px; }
    .history-list { margin: 4px 0 0 14px; padding: 0; font-size: 11px; color: #475569; }
    .history-list li { margin-bottom: 2px; }

    .badge { display: inline-block; padding: 2px 6px; border-radius: 10px; font-size: 10px; text-transform: uppercase; letter-spacing: 0.04em; }
    .badge-warning { background: #fef3c7; color: #92400e; }

    .empty-state { font-size: 12px; color: #6b7280; font-style: italic; }
  .truncation-note { font-size: 11px; color: #94a3b8; margin-top: 4px; }

    .feedback-category { border: 1px solid #e2e8f0; border-radius: 12px; padding: 12px 14px; margin-bottom: 12px; background: #ffffff; }
    .feedback-block { border: 1px solid #e2e8f0; border-radius: 10px; padding: 10px 12px; margin-bottom: 8px; background: #f8fafc; }
    .feedback-header { display: flex; justify-content: space-between; gap: 10px; font-size: 12px; color: #334155; margin-bottom: 6px; }
    .feedback-score { font-weight: 800; color: #500000; }
    .feedback-comments { font-size: 12px; color: #102a43; white-space: pre-wrap; overflow-wrap: anywhere; word-break: break-word; }
    .feedback-comments p { margin: 0 0 6px 0; overflow-wrap: anywhere; word-break: break-word; }
    .feedback-empty { font-size: 12px; color: #64748b; font-style: italic; }
    .feedback-meta { margin-top: 4px; font-size: 11px; }

    /* Competency summary table */
    .competency-table { width: 100%; border-collapse: collapse; margin: 10px 0 16px; font-size: 12px; page-break-inside: auto; }
    .competency-table thead { display: table-header-group; }
    .competency-table tfoot { display: table-footer-group; }
    .competency-table tr { page-break-inside: avoid; break-inside: avoid; }
    .competency-table th, .competency-table td { border: 1px solid #e2e8f0; padding: 8px 10px; vertical-align: top; page-break-inside: avoid; }
    .competency-table th { background: #f8fafc; color: #0f172a; font-weight: 700; }
    .competency-table td.numeric { text-align: center; white-space: nowrap; }
    .competency-table td:first-child { word-break: break-word; overflow-wrap: anywhere; }

    /* Footer */
    .pdf-footer { position: fixed; bottom: 6px; left: 0; right: 0; text-align: center; font-size: 11px; color: #8b99a6; }

    /* Small helpers */
    .muted { color: #6b7785; }
  </style>
</head>
<body>
  <div class="pdf-header">
    <div class="pdf-header-left">
      <span class="pdf-logo-badge">
        <%= image_tag(pdf_image_data_uri("tamu-logo.png"), class: "pdf-logo", alt: "Texas A&M University") %>
      </span>
      <div class="pdf-brand"><%= content_for?(:pdf_brand) ? yield(:pdf_brand) : "Health — Survey Response" %></div>
    </div>
    <div class="pdf-header-right"></div>
  </div>

  <div class="pdf-meta-block"><%= content_for?(:pdf_meta) ? yield(:pdf_meta) : "Generated: #{Time.current.strftime('%Y-%m-%d %H:%M')}" %></div>

  <%= yield %>

  <div class="pdf-footer">This report was generated by the MHA Survey Portal.</div>
</body>
</html>
