<% content_for :title, "Login Page - MHA Program" %>

<% content_for :header do %>
  <%= render "shared/login_navbar" %>
<% end %>

<section class="w-full max-w-4xl rounded-3xl bg-white px-6 py-12 text-center shadow-sm ring-1 ring-slate-200 md:px-10">
  <h1 class="text-3xl font-semibold text-[#500000] md:text-4xl">MHA Program Portal</h1>
  <p class="mt-3 text-base text-slate-600 md:text-lg">Master of Health Administration &mdash; Student and Advisor access</p>
</section>

<section id="login-sections" class="mt-10 w-full max-w-5xl">
  <div class="flex w-full flex-col gap-6 rounded-3xl bg-white px-8 py-10 text-center shadow-xl ring-1 ring-slate-200 md:flex-row md:items-stretch md:gap-0 md:px-12 md:py-14">
    <div id="student-login" class="flex flex-1 flex-col items-center justify-center px-4">
      <h2 class="text-2xl font-semibold text-slate-800">Student Login</h2>
      <p class="mt-4 max-w-sm text-sm text-slate-600">Access your surveys, track requirements, and stay informed on program updates.</p>
      <%= form_with url: user_google_oauth2_omniauth_authorize_path, method: :post, data: { turbo: "false" }, class: "mt-6 w-full max-w-xs" do |form| %>
        <%= form.hidden_field :role, value: 'student' %>
        <%= form.submit "Sign in with Google", class: "w-full rounded-md bg-[#500000] py-3 text-sm font-semibold text-white transition hover:bg-[#400000] focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[#500000]" %>
      <% end %>
    </div>

    <div class="mx-auto hidden h-full w-px bg-slate-200 md:block"></div>
    <div class="mx-auto h-px w-3/4 bg-slate-200 md:hidden"></div>

    <div id="advisor-login" class="flex flex-1 flex-col items-center justify-center px-4">
      <h2 class="text-2xl font-semibold text-slate-800">Advisor Login</h2>
      <p class="mt-4 max-w-sm text-sm text-slate-600">Manage student rosters, review feedback, and monitor survey responses.</p>
      <%= form_with url: user_google_oauth2_omniauth_authorize_path, method: :post, data: { turbo: "false" }, class: "mt-6 w-full max-w-xs" do |form| %>
        <%= form.hidden_field :role, value: 'advisor' %>
        <%= form.submit "Sign in with Google", class: "w-full rounded-md bg-[#500000] py-3 text-sm font-semibold text-white transition hover:bg-[#400000] focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[#500000]" %>
      <% end %>
    </div>
  </div>
</section>
