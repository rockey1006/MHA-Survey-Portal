<%= form_with(model: competency, local: true) do |form| %>
  <% if competency.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(competency.errors.count, "error") %> prohibited this competency from being saved:</h2>

      <ul>
        <% competency.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :competency_id, style: "display: block" %>
    <%= form.number_field :competency_id %>
  </div>

  <div>
    <%= form.label :survey_id, style: "display: block" %>
    <%= form.number_field :survey_id %>
  </div>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_field :description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
