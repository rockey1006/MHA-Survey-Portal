<div class="survey-container survey-show mx-auto w-full space-y-8 px-4 sm:px-6 lg:px-4" style="max-width: 90rem;">
  <% success_notice = notice.presence || flash[:notice].presence %>
  <% if success_notice.present? %>
    <p class="text-sm font-medium text-emerald-700"><%= success_notice %></p>
    <script>
      (function() {
        var ran = false;
        function scrollToTop() {
          if (ran) return;
          ran = true;
          window.scrollTo({ top: 0, behavior: "smooth" });
        }
        if (document.readyState === "complete" || document.readyState === "interactive") {
          scrollToTop();
        } else {
          document.addEventListener("DOMContentLoaded", scrollToTop, { once: true });
        }
        document.addEventListener("turbo:load", scrollToTop, { once: true });
      })();
    </script>
  <% end %>

  <div class="flex flex-wrap items-center gap-3">
    <%= link_to "← Back", student_dashboard_path, class: tailwind_button_classes(:secondary) %>
  <%= link_to "Download as PDF", download_survey_response_path(@survey_response, token: @survey_response.signed_download_token), class: tailwind_button_classes(:primary), target: "_blank", rel: "noopener noreferrer" %>
  </div>

  <%= render @survey_response %>

  <div class="flex flex-wrap items-center gap-3">
    <%= link_to "← Back", student_dashboard_path, class: tailwind_button_classes(:secondary) %>
  <%= link_to "Download as PDF", download_survey_response_path(@survey_response, token: @survey_response.signed_download_token), class: tailwind_button_classes(:primary), target: "_blank", rel: "noopener noreferrer" %>
  </div>
</div>
